<!DOCTYPE html>
<html>
<head>
  <title>Monarch Diseases</title>

  {{{includes.analytics}}}

  <!-- CSS. -->
  {{#pup_tent_css_libraries}}
  <link rel="stylesheet" type="text/css" href="{{{.}}}">
  {{/pup_tent_css_libraries}}

  <!-- JS variables. -->
  {{#pup_tent_js_variables}}
  <script type="text/javascript">var {{name}} = {{{value}}};</script>
  {{/pup_tent_js_variables}}

  <!-- JS libraries. -->
  {{#pup_tent_js_libraries}}
  <script type="text/javascript" src="{{{.}}}"></script>
  {{/pup_tent_js_libraries}}
  
  {{{disPhenoGraph}}}

  <link rel="icon" href="/image/favicon.ico" type="image/x-icon">
  <meta charset="utf-8"/>
</head>
<body style="padding-top: 50px;">
  {{{includes.navbar}}}
  <div class="container-fluid" style="margin: 1em;">
    <!-- Open first row section. -->
    <div class="row">
      <!-- Disease spotlight -->
      <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
        <div class="panel panel-default">
          <h4>Disease Spotlight: {{{spotlight.label}}}</h4>
          <div>
            <p>{{spotlight.description}}</p>
            <ul>
        <li>Heritability: {{{spotlight.heritability}}}</li>
                <li>Phenotypes: {{{spotlight.phenotypes}}}</li>
                <li>Genes: {{{spotlight.genes}}}</li>
                <li>Models: {{spotlight.model_count}} 
        <!--({{spotlight.model_with_phenotype_count}} with phenotypes) -->
            </li>
                <li>Publications: {{spotlight.publication_count}}</li>
            </ul>
          </div>
          <p class="text-right">{{{spotlight.link}}}</p>
        </div>
        <!-- Getting started area or disease browser -->
        <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
          <div class="panel panel-default">
            <p>Some kind of getting started area? Or maybe disease browser</p>
          </div>
        </div>
      </div>
      <!-- Close first row section. -->
      <!-- Open second row section. -->
      <div class="row">
        <!-- Recent updates -->
        <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
          <div class="panel panel-default">
            <h4>Recent Updates</h4>
            {{#blog_results}}
            <ul>
              <li>
                <a href="{{link}}">{{title}}</a> <small>({{date}})</small>
              </li>
            </ul>
            {{/blog_results}}
            {{#blog_results.length}}
            <p class="text-right"><a href="http://monarch-initiative.blogspot.com/search/label/test">More</a></p>
            {{/blog_results.length}}
            {{^blog_results}}
            <p>No news is good news.</p>
            {{/blog_results}}
          </div>
        </div>
        <!-- Annotation Graph-->
        <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
          <div class="panel panel-default">
              <div class="disease-pheno-container"></div>
               <br></br><br></br><br></br><br></br><br></br>
               <br></br><br></br><br></br><br></br><br></br>
               <br></br><br></br>
          </div>
        </div>
      </div>
      <!-- Close second row section. -->
    </div>
  </body>
</html>
