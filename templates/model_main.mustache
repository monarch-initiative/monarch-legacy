{{{includes.navbar}}}
<div class="container-fluid" style="margin: 1em;">

  <div class="text-center">
    <h1>
      Model
    </h1>
  </div>

  <!-- Open first row section. -->
  <div class="row">

    <!-- Blurb. -->
    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-3">
      <div class="panel panel-default">
	<p>
	  Monarch <b>Model</b> integrates information from a variety
	  of sources. Each specific page includes the abnormal genes
	  and variations, phenotypes, and aggregated literature, with
	  external links to relevant resources. Here we summarizes
	  the curated genotype-phenotype data in the system.
	</p>
      </div>
    </div>

    <!-- Annotation Graph-->
    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-9">
      <div class="panel panel-default">
        <div class="pheno-geno-container"></div>
	<p class="datagraph-caption">
	  This <em>interactive graph</em> displays the number of
	  phenotype associations for mouse (genotypes) per major
	  phenotypic category. You can explore the distribution of
	  more specific phenotypes by clicking on the wedge symbol, or
	  visit the phenotype page itself by clicking on the phenotype
	  name. You can navigate back to less-specific phenotypes by
	  clicking on the breadcrumbs listed in the top left-hand
	  corner.
	</p>
      </div>
    </div>

    <!-- Spotlight -->
    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <div class="panel panel-default">
        <h4>Model Spotlight: {{{spotlight.label}}}</h4>
        <div>
          <p>{{spotlight.description}}</p>
          <ul>
            <li>Taxon: {{spotlight.taxon}} </li>
            <li>Genes: {{{spotlight.genes}}} </li>
            <li>Phenotypes: {{{spotlight.phenotypes}}}</li>
            <li>Publications: {{spotlight.publication_count}}</li>
          </ul>
	</div>
        <p class="text-right">{{{spotlight.link}}}</p>
      </div>
    </div>

  </div>
</div>
</body>
</html>
