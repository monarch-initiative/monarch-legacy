<!DOCTYPE html>
<html>
<head>
  <title>Monarch Disease: {{label}} ({{id}})</title>
  {{#scripts}}
  <script type="text/javascript" src="{{url}}"></script>
  <script type="text/javascript">
    $(document).ready(function() {
      //I think the RingoJS code is calling the document.ready() each time a section
      //of code returns.  This causes my svg to get recreated several times.
      //the loaded_once variable flags this event to ensure it only happens once
      if (loaded_once != 1) {
      createSvg();
      loadData();
      init();
      createModelRegion();
      update();
      }
    });
  </script>
  {{/scripts}}
  {{#stylesheets}}
  <link rel="stylesheet" type="text/css" href="{{url}}"></link>
  {{/stylesheets}}
  <link rel="stylesheet" type="text/css" href="../css/disease.css"></link>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta charset=”utf-8”>
</head>
<body>
  {{{includes.navbar}}}
  <div class="container">
  <div class="sidebar">
    {{{includes.navlistdisease}}}
  </div>
  <div class="content">
    <div class="jumbotron">
      <div id="description">
        <h1>Disease: {{label}}</h1>
        <a href="{{{base_url}}}.json" class="btn btn-success">JSON</a>
        <!-- <a href="{{{base_url}}}.rdf" class="btn btn-success">RDF</a> -->
        <a href="{{{base_url}}}.nt" class="btn btn-success">n-triples</a>
        <p>
        {{comments}}
        </p>
        <a href="https://github.com/cmungall/monarch-app/issues/3">todo - tidy this</a>
        <p>
        <i>AKA: </i> {{has_exact_synonym}}
        </p>
        <div id="ids">
        <i>IDs: </i> {{database_cross_reference}}
        </div>
      </div>
    </div>
    <div id="ontology" class="category">
      <h3>Ontology</h3>
      <p>
      <table class="{{css.table}}">
      {{{ontNavBox}}}
      </table>
      </p>
    </div>
    <div id="phenotype" class="category">
      <h3>Phenotypes</h3>
      <a href="{{{base_url}}}/phenotype_associations.json" class="btn btn-success">JSON</a>
      <!--  <a href="{{{base_url}}}/phenotype_associations.rdf" class="btn btn-success">RDF</a> -->
      <a href="{{{base_url}}}/phenotype_associations.nt" class="btn btn-success">n-triples</a>
      <span class="text-warning">May be incomplete/with false
      positives. See: <a href="https://support.crbs.ucsd.edu/browse/LAMHDI-140"></a>
      </span>
      <table class="{{css.table}}">
      {{{phenotypeTable}}}
      </table>
    </div>
    <div id="gene" class="category">
      <h3>Genes</h3>
      <table class="{{css.table}}">
      {{{geneTable}}}
      </table>
    </div>
    <div id="allele" class="category">
      <h3>Alleles</h3>
      <table class="{{css.table}}">
      {{{alleleTable}}}
      </table>
    </div>
    <div id="model" class="category">
      <h3>Models</h3>
      <a href="{{{base_url}}}/models.json" class="btn btn-success">JSON</a>
      <!--  <a href="{{{base_url}}}/models.rdf" class="btn btn-success">RDF</a> -->
      <a href="{{{base_url}}}/models.nt" class="btn btn-success">n-triples</a>
      <table class="{{css.table}}">
      {{{modelTable}}}
      </table>
    </div>
    <div id="sim" class="category">
      <h3>Similar Diseases</h3>
      <a href="{{{base_url}}}/sim.json" class="btn btn-success">JSON</a>
      <table class="{{css.table}}">
      {{{simTable}}}
      </table>
      </div>
    </div>
  </div>
</body>
</html>
