<!DOCTYPE html>
<html>
<head>
    <title>Monarch Disease: {{label}} ({{id}})</title>
    {{#scripts}}
    <script type="text/javascript" src="{{url}}"></script>
        <script type="text/javascript">
       $( document ).ready(function() {
       //I think the RingoJS code is calling the document.ready() each time a section
       //of code returns.  This causes my svg to get recreated several times.
       //the loaded_once variable flags this event to ensure it only happens once
          if (loaded_once != 1) {
	          createSvg();
	          loadData();
			  init();
			  createModelRegion();
			  update();
	          
          }
       });
    </script>
    {{/scripts}}
    {{#stylesheets}}
    <link rel="stylesheet" type="text/css" href="{{url}}"></link>
    {{/stylesheets}}
    <link rel="stylesheet" type="text/css" href="../css/monarch-common.css"></link>
    <script src="../js/tabs.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset=”utf-8”>
</head>
<body>
    {{{includes.navbar}}}
    <div class="container">
    <div class="sidebar">
        {{{includes.navlistdisease}}}
    </div>
    <div class="content">
        <div class="jumbotron">
            <div id="description">
                <h1>Disease: {{label}}</h1>
                <p>
                {{comments}}
                </p>
                <a href="https://github.com/cmungall/monarch-app/issues/3">todo - tidy this & add description of disease</a>
                <div class="ids">
                    <b>AKA: </b>
                    {{#has_exact_synonym}}
                    {{.}},
                    {{/has_exact_synonym}}
                </div>
                <div class="ids">
                    <b>IDs: </b>
                    {{#database_cross_reference}}
                    {{.}},
                    {{/database_cross_reference}}
                </div>
            </div>
        </div>
        <div id="complete-info">
            <div id="categories">
                <a href="#overview"><div id="initialtab" class="tab overtab">Overview</div></a>
                <a href="#ontology"><div class="tab onttab">Ontology</div></a>
                <a href="#phenotype"><div class="tab phentab">Phenotypes</div></a>
                <a href="#gene"><div class="tab gentab">Genes</div></a>
                <div class="second-row-divider"></div>
                <a href="#allele"><div class="tab alletab">Alleles</div></a>
                <a href="#model"><div class="tab modtab">Model</div></a>
                <a href="#sim"><div class="tab simtab">Similarity</div></a>
                <a href="#down"><div class="tab downtab">Download</div></a>
            </div>
            <div id="overview" class="category">
                <h3>To Do</h3>
                <p> This panel needs to have its own set of tables with a couple data
                points from each section. </p>
            </div>
            <div id="ontology" class="category">
                <h3>Ontology</h3>
                <p>
                <table class="{{css.table}}">
                {{{ontNavBox}}}
                </table>
                </p>
            </div>
            <div id="phenotype" class="category">
                <h3>Phenotypes</h3>
                <span class="text-warning">May be incomplete/with false
                positives. See: <a href="https://support.crbs.ucsd.edu/browse/LAMHDI-140"></a>
                </span>
                <table class="{{css.table}}">
                {{{phenotypeTable}}}
                </table>
            </div>
            <div id="gene" class="category">
                <h3>Genes</h3>
                <table class="{{css.table}}">
                {{{geneTable}}}
                </table>
            </div>
            <div id="allele" class="category">
                <h3>Alleles</h3>
                <table class="{{css.table}}">
                {{{alleleTable}}}
                </table>
            </div>
            <div id="model" class="category">
                <h3>Models</h3>
                <table class="{{css.table}}">
                {{{modelTable}}}
                </table>
            </div>
            <div id="sim" class="category">
                <h3>Similar Diseases</h3>
                <table class="{{css.table}}">
                {{{simTable}}}
                </table>
            </div>
            <div id="down" class="category">
                <h3>Download</h3>
                <div class="buttonsegment">Disease Information</div>
                <a href="{{{base_url}}}.json" class="btn btn-success">JSON</a>
                <!-- <a href="{{{base_url}}}.rdf" class="btn btn-success">RDF</a> -->
                <a href="{{{base_url}}}.nt" class="btn btn-success">n-triples</a>
                <div class="buttonsegment">Phenotypes</div>
                <a href="{{{base_url}}}/phenotype_associations.json" class="btn btn-success">JSON</a>
                <!--    <a href="{{{base_url}}}/phenotype_associations.rdf" class="btn btn-success">RDF</a> -->
                <a href="{{{base_url}}}/phenotype_associations.nt" class="btn btn-success">n-triples</a>
                <div class="buttonsegment">Model</div>
                <a href="{{{base_url}}}/models.json" class="btn btn-success">JSON</a>
                <!--    <a href="{{{base_url}}}/models.rdf" class="btn btn-success">RDF</a> -->
                <a href="{{{base_url}}}/models.nt" class="btn btn-success">n-triples</a>
                <div class="buttonsegment">Similar Diseases</div>
                <a href="{{{base_url}}}/sim.json" class="btn btn-success">JSON</a>
            </div>
        </div>
    </div>
</body>
</html>
