<!DOCTYPE html>
<html>
  <head>
    <title>Search Results: {{term}}</title>
    {{#scripts}}
    <script type="text/javascript" src="{{url}}"></script>
    {{/scripts}}
    {{#stylesheets}}
    <link rel="stylesheet" type="text/css" href="{{url}}"></link>
    {{/stylesheets}}
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset=”utf-8”> 
  </head>
  <body>
    <div class="container">
      {{{includes.navbar}}}
      <p class="text-warning">Sorry search is so slow! We're working
        on it (TODO: file JIRA ticket)</p>
      <div class="row">
        <div class="col-lg-2">
          {{{includes.navlist}}}
        </div>
        <div class="col-lg-9">
          <h1>Search Results: {{term}}</h1>
          <p>Download: <a href="{{{download.json}}}">JSON</a></p>
          <table class="{{css.table}}">
            {{{resultsTable}}}
          </table>
        </div>
      </div>
    </div>
  </body>
</html>
