language: javascript

# command to install dependencies
install:
  - "./install.sh"

# command to run tests
script: 
  - "make test"
  - "pip install selenium"
  - "pip install behave"
  - "pip install jsonpath-rw"
  - "cd tests/behave"
  - "source bin/activate"
  - "behave"

# whitelist
branches:
  only:
    - master

notifications:
  email:
    - kshefchek@gmail.com
